#!/usr/bin/env sh

if ! command -v carthage > /dev/null; then
	printf 'Carthage is not installed.\n'
	printf 'See the thing for the installing'
	exit 1
fi

carthage update --platform iOS --use-submodules --no-use-binaries
